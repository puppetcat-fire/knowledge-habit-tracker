# 安全指南 - 知识习惯跟踪器

## 安全架构概述

知识习惯跟踪器是一个**纯本地运行**的应用，设计时考虑了隐私和安全。以下是安全特性的详细说明。

## 运行模式与风险等级

### 1. 本地模式（默认推荐）
- **配置**: `HOST=localhost`
- **风险等级**: 🔵 极低
- **说明**: 仅本机可以访问，无法从网络访问
- **适用场景**: 个人使用、开发测试

### 2. 局域网模式
- **配置**: `HOST=0.0.0.0`
- **风险等级**: 🟡 中等
- **说明**: 同一网络内的设备可以访问
- **安全要求**: 建议启用CORS和API密钥
- **适用场景**: 内部团队使用、多设备访问

### 3. 公网模式（不推荐）
- **配置**: `HOST=0.0.0.0` + 端口转发
- **风险等级**: 🔴 高
- **说明**: 互联网上的任何设备都可以访问
- **安全要求**: 必须启用所有安全措施 + HTTPS
- **警告**: 除非必要，否则不要暴露到公网

## 安全特性

### 已实现的安全措施

#### 1. 输入验证与清理
- 所有用户输入都经过清理，移除危险字符
- 字段长度限制防止缓冲区溢出
- 类型验证确保数据格式正确

#### 2. HTTP安全头
- 使用Helmet设置安全HTTP头
- 内容安全策略(CSP)防止XSS攻击
- 禁止MIME类型嗅探

#### 3. 可选安全特性
- **CORS控制**: 限制可访问的来源
- **API密钥**: 简单的认证机制
- **速率限制**: 防止暴力攻击

#### 4. 数据隐私
- Bug报告中的敏感信息（IP、用户代理）仅用于日志
- API响应中隐藏敏感字段
- 本地文件存储，数据不离境

### 安全配置指南

#### 最低安全配置（本地使用）
```bash
# .env 文件
PORT=3000
HOST=localhost
ENABLE_CORS=false
ENABLE_RATE_LIMIT=false
# API_KEY 留空
```

#### 增强安全配置（局域网使用）
```bash
# .env 文件
PORT=3000
HOST=0.0.0.0
ENABLE_CORS=true
CORS_ORIGIN=http://localhost:8082
API_KEY=your-secure-api-key
ENABLE_RATE_LIMIT=true
```

#### 生产环境配置（如需公网访问）
```bash
# .env 文件
PORT=3000
HOST=0.0.0.0
ENABLE_CORS=true
CORS_ORIGIN=https://your-domain.com
API_KEY=strong-random-key-here
ENABLE_RATE_LIMIT=true

# 额外要求：
# 1. 使用Nginx/Apache反向代理
# 2. 配置HTTPS证书
# 3. 设置防火墙规则
# 4. 启用系统级监控
```

## 常见问题

### Q: 为什么纯本地应用还需要安全措施？
A: 即使本地运行，也需要防范：
1. 浏览器安全漏洞（XSS、CSRF）
2. 本地恶意软件滥用API
3. 未来可能扩展到网络访问
4. 防止意外配置错误导致暴露

### Q: 如何知道我的服务是否安全？
A: 检查以下指标：
1. 只监听需要的端口
2. 有适当的访问控制
3. 输入数据经过验证
4. 错误信息不泄露敏感数据

### Q: 如果我想在公网使用怎么办？
A: **强烈不建议**，但如果必须：
1. 使用安全版本 (`server-secure.js`)
2. 启用所有安全选项
3. 配置HTTPS（必须）
4. 使用反向代理（Nginx）
5. 设置防火墙规则
6. 定期更新和监控

### Q: 如何检查当前安全状态？
A: 访问健康检查端点：
```
http://localhost:3000/health
```
查看安全配置部分。

## 应急响应

### 发现安全漏洞时
1. **立即停止服务**
2. **检查日志**：查看异常访问
3. **评估影响**：确定泄露的数据
4. **修复漏洞**：更新代码或配置
5. **恢复服务**：确认安全后重启

### 联系方式
如有安全问题，请通过项目Issue报告。

## 更新日志

### v1.0.0 (2026-03-05)
- 初始安全版本
- 添加输入验证和清理
- 实现可选的CORS和API密钥
- 添加安全HTTP头

---

**安全第一，隐私至上**